export interface SystemConfig {
  // Define the structure of a single system configuration item
  [key: string]: any; // Replace with actual configuration properties
}

export interface OrganizationConfig {
  // Define the structure of the organization configuration
  [key: string]: any; // Replace with actual properties
}

export interface OrganizationStatus {
  // Define the structure of the organization status
  [key: string]: any; // Replace with actual properties
}

export interface OrganizationHistory {
  // Define the structure of an organization history item
  [key: string]: any; // Replace with actual properties
}